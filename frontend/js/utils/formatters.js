// TODO: Define per-sensor trend thresholds.
// For now, trend can be based on simple comparison:
// current > previous = up
// current < previous = down
// current === previous = unchanged
// Later, each sensor may need its own threshold so tiny changes are treated as unchanged.
